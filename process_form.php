<?php
if ($_SERVER["REQUEST_METHOD"] === "POST") {
    $postContent = $_POST["post-content"];
    // Save $postContent to a database or file
}
// Redirect back to the page or show a confirmation message
?>
